import React from 'react';
import './footer.scss';

function Footer() {
    return (
        <footer className="footer">
            {/* Contenu du footer */}
        </footer>
    );
}

export default Footer;