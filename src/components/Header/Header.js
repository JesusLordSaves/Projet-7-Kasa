import React from 'react';
import './header.scss';

function Header() {
    return (
        <header className="header">
            {/* Contenu de l'en-tête */}
        </header>
    );
}

export default Header;